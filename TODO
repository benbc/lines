Add fsrs details to lines (pass/fail)
Pick due lines, if there are any, before new ones, earliest due date first
Less time-consuming mode for lines in "review" state
Add easy/hard
Add way of flagging lines failed in rehearsals etc

Deploy on internet
Mobile version

Prune orphaned lines from database
