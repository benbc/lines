maybe: Use same logic in extending as recordSuccess so we don't extend over lots of lines that will be ignored
maybe: change sort order to go through a scene in order
maybe: vary algorithm for lines that are repeatedly forgotten (slower/non-determinstic ease bumping?)

Deploy on internet
Mobile version
Transfer state between clients

Run whole scenes
Undo
Add way of flagging lines failed in rehearsals etc
Display summary information
Indicate hard lines
Remove ingestion
