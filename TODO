Relearn should extend back up the script instead of taking a slice of lines because lines might not be long enough

Deploy on internet
Mobile version
Transfer state between clients

Undo
Add way of flagging lines failed in rehearsals etc
Display summary information
Indicate hard lines
